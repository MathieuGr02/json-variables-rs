[package]
name = "json_variables"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"

regex = "1"

thiserror = "2.0.12"

[dev-dependencies]
rstest = "0.25.0"
